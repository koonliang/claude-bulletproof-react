NODE_ENV=test
PORT=3002
JWT_SECRET=test-super-secret-jwt-key-for-testing-only
JWT_EXPIRES_IN=7d

# Test Database - Mocked (no real database needed for testing)
DATABASE_PROVIDER=mysql
DATABASE_URL=mysql://mock:mock@localhost:3306/mock_test

# CORS
CORS_ORIGIN=http://localhost:3000